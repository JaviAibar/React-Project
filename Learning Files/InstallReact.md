# Using React by installing instead of by CDN

## Packages

You have to install via npm (or whatever environment you are at).
Remove the CDN references.
Import React in every jsx file:
```js
import React from "react"
```